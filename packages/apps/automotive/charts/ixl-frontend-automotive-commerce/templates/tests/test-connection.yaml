apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ixl-frontend-automotive-commerce.fullname" . }}-test-connection"
  labels:
    {{- include "ixl-frontend-automotive-commerce.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ixl-frontend-automotive-commerce.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
