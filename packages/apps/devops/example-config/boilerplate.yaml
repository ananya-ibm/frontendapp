apiVersion: apps/v1
kind: Deployment
metadata:
  name: ixl-frontend-devops-deployment
spec:
  selector:
    matchLabels:
      app: ixl-frontend-devops
  replicas: 1
  template:
    metadata:
      labels:
        app: ixl-frontend-devops
    spec:
      containers:
        - name: boilerplate
          image: uk.icr.io/exo-devops-devops/ixl-frontend-devops
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          envFrom: 
            - configMapRef:
                name: ixl-frontend-devops-env
---              
apiVersion: v1
kind: Service
metadata:
  name: ixl-frontend-devops-service
spec:
  selector:
    app: ixl-frontend-devops
  ports:
    - protocol: "TCP"
      port: 3000
      targetPort: 3000
  type: NodePort
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: ixl-frontend-devops-route
spec:
  to:
    kind: Service
    name: ixl-frontend-devops-service
    weight: 100
  port:
    targetPort: 3000
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None